{
    "$schema": "https://json.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "minify-heatmap": "terser public/js/heatmap-simple.js --compress --mangle --output public/js/heatmap.min.js",
        "build-all": "npm run build && npm run minify-heatmap"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.8.2",
        "concurrently": "^9.0.1",
        "laravel-echo": "^2.2.0",
        "laravel-vite-plugin": "^1.2.0",
        "pusher-js": "^8.4.0",
        "tailwindcss": "^4.0.0",
        "terser": "^5.43.1",
        "vite": "^6.2.4"
    }
}
